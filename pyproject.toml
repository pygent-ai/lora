[project]
name = "lora"
version = "0.3.2"
description = "Local agent development and evaluation workbench"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "pygent-ai==0.3.5",
    "uvicorn>=0.32.0",
]

[dependency-groups]
dev = [
    "pyright>=1.1.400",
    "pytest>=9.0.3",
    "pytest-asyncio>=0.23.0",
]
credentials = [
    "keyring>=25.0.0",
]

[project.scripts]
lora = "lora.cli:main"
lora-api = "lora_api.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lora", "src/lora_api"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
