# Copy secrets to ~/.lora/credentials.env (recommended).
# Do not commit real API keys into the repository.

DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
# Optional. Set to false only for development endpoints with untrusted certificates.
PYGENT_VERIFY_SSL=true
