# PySide GUI Legacy Area

The archived PySide6 GUI source lives in `legacy/pyside_gui/src/gui`.

This area is retained as reference material for the Electron + React rebuild. It may keep legacy imports and scripts until there is a specific need to run or port it.

The compatibility entry point is still available as:

```powershell
uv run --group legacy-gui python -m gui
```
