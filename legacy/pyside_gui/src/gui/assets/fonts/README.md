# External Fonts

Place `.ttf`, `.ttc`, or `.otf` font files in this directory to make them appear in the standalone Resources GUI.

The Resources catalog also scans:

- `C:/Windows/Fonts`
- `resources/fonts`
- `.lora/fonts`

Restart `uv run lora-resources` after adding font files.
